# Schema for the theme setting configuration file of the CANADA Bootstrap theme.

canada_bootstrap.settings:
  type: theme_settings
  label: 'CANADA Bootstrap settings'
  mapping:
    cdn_provider:
      type: string
      label: 'CDN Provider'
    breadcrumb_home:
      type: integer
      label: 'Breadcrumb Home'
