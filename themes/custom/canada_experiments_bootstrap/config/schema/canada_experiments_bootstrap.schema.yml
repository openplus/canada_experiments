# Schema for the theme setting configuration file of the Canada Experiments theme.

canada_experiments_bootstrap.settings:
  type: theme_settings
  label: 'Canada Experiments settings'
  mapping:
    cdn_provider:
      type: string
      label: 'CDN Provider'
    breadcrumb_home:
      type: integer
      label: 'Breadcrumb Home'
